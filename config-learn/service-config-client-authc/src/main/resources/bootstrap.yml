spring:
  cloud:
    config:
      uri: http://user:password123@localhost:8080
      profile: dev
      label: master   # 当configserver的后端存储是Git时，默认就是master 
  application:
    name: foobar
    
    
    
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: service-config-server-eureka
      username: user
      password: password123
  application:
    name: foobar
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:8761/eureka
  instance:
    prefer-ip-address: true
    
    
    
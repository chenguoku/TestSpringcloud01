server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenguoku/SpringCloud-config-repository
          username: 
          password:

encrypt:
  keyStore:
    location: classpath:/server.jks
    password: letmein
    alias: mytestkey
    secret: changeme
